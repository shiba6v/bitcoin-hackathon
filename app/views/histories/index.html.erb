<p id="notice"><%= notice %></p>

<h1>Histories</h1>

<table>
  <thead>
    <tr>
      <th>Nonce start</th>
      <th>Nonce end</th>
      <th>Prev block</th>
      <th>Finish</th>
    </tr>
  </thead>

  <tbody>
    <% @histories.each do |history| %>
      <tr>
        <td><%= history.nonce_start %></td>
        <td><%= history.nonce_end %></td>
        <td><%= history.prev_block %></td>
        <td><%= history.finish %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
